name: Build Flet APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 🟢 تحديث لـ v4 لدعم الأنظمة الجديدة
      - uses: actions/checkout@v4

      # 🟢 تحديث لـ v5 ليكون متوافق 100%
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 🟢 تحديث لـ v4 لتجنب أي تنبيه
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Install Flet & Requests
        run: |
          pip install flet requests

      - name: Build APK
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_KEY }}
        run: |
          flet build apk --project-name "NIBRAS AI" --product "Nibras AI" --build-version "1.0.0"

      # 🟢 هادي ديجا مأمنة ف الـ v4 ومكادير حتى مشكل
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: nibras-apk
          path: build/apk/*.apk
